frappe.pages['airplane-flights'].on_page_load = function(wrapper) {
	var page = frappe.ui.make_app_page({
		parent: wrapper,
		title: 'Flights',
		single_column: true
	});

	
		//let field = page.add_field({

		//label: 'Destination Airport Code',
		//fieldtype: 'Read Only',
		//fieldname: 'destination_airport_code'

	//});
}